# Separate Public V2 Repo

Eddy V2 lives in a new public `lennoxsaint/eddy-v2` repo while the existing `lennoxsaint/eddy` repo remains public and unchanged. This avoids a destructive reset of the current proof history while giving V2 permission to start from a simpler architecture.
